# US-22 — Tasks

- [x] Spec folded into `current/` (requirements + design)
- [x] `packMergedModelsGlb` + `downloadExportZip({ mergeModels })`
- [x] On merge: per-model clip picks → Scene bake only; shared also as unprefixed sidecars
- [x] `ExportModal` + Download opens modal
- [x] Merge toggle gated on `previewModelIds.length >= 2`
- [x] Filename inputs: zip + merged / per-model basenames; Scene clip name + per-model clip Selects when merge on
- [ ] Manual: fight bake (Attack + HitReact) plays both characters from Scene clip
