# US-35 — Tasks

- [x] `src/utils/local-storage/` — keys + get/set number/string (safe)
- [x] `ResizableShell` + drag hook (horizontal / vertical, start / end, enabled)
- [x] Wire `CollapsibleAside` + Library / Settings `storageKey` (desktop only)
- [x] Wire `EditorPreviewBar` (desktop + mobile)
- [ ] Manual: resize Library for long bone groups; resize Tracks bar on mobile; reload restores sizes
