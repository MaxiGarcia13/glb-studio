---
import BaseLayout from '@/layouts/base-layout.astro';
import { EditorPreview, EditorLibrarySidebar, EditorSettingsSidebar } from '@/modules/editor-shell';
---

<BaseLayout>
  <main class="flex h-screen w-screen overflow-hidden bg-canvas text-fg">
    <EditorLibrarySidebar client:only="react" />
    <EditorPreview client:only="react" />
    <EditorSettingsSidebar client:only="react" />
  </main>
</BaseLayout>
